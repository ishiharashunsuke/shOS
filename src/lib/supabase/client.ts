// Placeholder — no Supabase project is connected yet.
// Once one exists, implement using `createBrowserClient` from "@supabase/ssr"
// for use in Client Components (e.g. Realtime subscriptions, AI Lab chat).

export function createBrowserSupabaseClient(): never {
  throw new Error("Supabase is not configured yet. See src/lib/supabase/client.ts.");
}
