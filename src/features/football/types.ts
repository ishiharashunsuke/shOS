export type Player = { slug: string; name: string; teamSlug: string; position: string };
export type Team = { slug: string; name: string; league: string };
export type Formation = { id: string; teamSlug: string; name: string };
export type Analysis = { slug: string; title: string };
