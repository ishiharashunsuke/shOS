import type { CareerEntry, Profile, Skill } from "../types";

// Placeholder — structure only. About content changes rarely, so this may
// stay as static data (no Supabase table) even after other features migrate.

export function getProfile(): Profile | undefined {
  return undefined;
}

export function getCareerEntries(): CareerEntry[] {
  return [];
}

export function getSkills(): Skill[] {
  return [];
}
