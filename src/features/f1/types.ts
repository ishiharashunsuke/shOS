export type Driver = { slug: string; name: string; teamSlug: string };
export type Team = { slug: string; name: string };
export type Circuit = { slug: string; name: string; country: string };
export type Race = { slug: string; name: string; season: number; circuitSlug: string };
