// Placeholder — replace with the output of
// `npx supabase gen types typescript --project-id <id>` once the Supabase
// project exists. This is the one source of truth for raw table shapes;
// each feature's types.ts maps from these into its own domain-shaped types.
export type Database = Record<string, never>;
